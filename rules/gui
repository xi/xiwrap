setenv HOME
setenv XDG_RUNTIME_DIR

setenv DISPLAY
setenv WAYLAND_DISPLAY
setenv XDG_CURRENT_DESKTOP
ro-bind $XDG_RUNTIME_DIR/$WAYLAND_DISPLAY

ro-bind /etc/fonts
ro-bind /usr/share/fonts
ro-bind /usr/share/icons
ro-bind /usr/share/themes/
ro-bind $HOME/.local/share/fonts
ro-bind $HOME/.local/share/icons
ro-bind $HOME/.local/share/themes
bind $HOME/.cache/fontconfig
bind $HOME/.cache/thumbnails

dbus-talk org.a11y.Bus

setenv GTK_USE_PORTAL 1
dbus-talk org.freedesktop.portal.Desktop
dbus-talk org.freedesktop.portal.Documents
