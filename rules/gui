setenv HOME
setenv XDG_RUNTIME_DIR

setenv DISPLAY
setenv WAYLAND_DISPLAY
setenv XDG_CURRENT_DESKTOP
ro-bind $XDG_RUNTIME_DIR/$WAYLAND_DISPLAY

ro-bind-try /etc/fonts
ro-bind-try /usr/share/fonts
ro-bind-try /usr/share/icons
ro-bind-try /usr/share/themes/
ro-bind-try $HOME/.local/share/fonts
ro-bind-try $HOME/.local/share/icons
ro-bind-try $HOME/.local/share/themes
bind-try $HOME/.cache/fontconfig
bind-try $HOME/.cache/thumbnails

dbus-talk org.a11y.Bus

setenv GTK_USE_PORTAL 1
dbus-talk org.freedesktop.portal.Desktop
dbus-talk org.freedesktop.portal.Documents
